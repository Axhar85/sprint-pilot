# SprintPilot

SprintPilot is an AI-assisted agile task board built with React and TypeScript.

## Local development

Install dependencies:

```bash
npm install
```

Start the development server:

```bash
npm run dev
```

Run the code-quality checks:

```bash
npm run lint
npm run build
```
